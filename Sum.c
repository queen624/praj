#include<stdio.h>
Int sum of digits(int n)
{
Int n==0
Return 0:

Return (n%10 + sum of digits(n/10));
}
Int main()
{
Int num=1234;
Int result=sum of digits(num);
Printf("sum of digits in %d is %d/n",n,result);
Return 0;
}
